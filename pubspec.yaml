name: my_tool_kit
description: "A new Flutter package project."
version: 0.0.1
homepage:

environment:
  sdk: '>=3.3.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  # 永久储存
  shared_preferences: ^2.2.3
  # 状态管理工具,路由
  get: ^4.6.6
  # 底部表单
  modal_bottom_sheet: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
flutter:
